# Render Blueprint — one-shot backend deploy.
# Dashboard: New + → Blueprint → connect Demiladepy/attest → Render reads this file.
# Non-secret config is baked in; `sync: false` vars are prompted for at deploy
# (never committed to git). Free plan, Docker, health-checked.
services:
  - type: web
    name: attest-api
    runtime: docker
    # Render retired the free web-service tier (Apr 2026). Starter is the entry
    # paid plan (~$7/mo) — cancel after the hackathon.
    plan: starter
    # dockerfilePath is relative to the REPO ROOT (not dockerContext); the build
    # context is backend/ so the Dockerfile's COPY paths resolve correctly.
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      # ── Non-secret config ──────────────────────────────
      - key: DEMO_MODE
        value: "true"
      - key: DEBUG
        value: "false"
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: TENANT_ID
        value: demo-workspace
      - key: B2_REGION
        value: us-east-005
      - key: B2_BUCKET
        value: attest-dma-2026
      - key: CORS_ORIGINS
        value: https://attest-black-two.vercel.app
      # ── Prompted at deploy (paste in Render UI) ─────────
      # Set to the Render URL after the first deploy, then redeploy:
      - key: API_PUBLIC_BASE_URL
        sync: false
      # Secrets — copy values from local backend/.env:
      - key: B2_KEY_ID
        sync: false
      - key: B2_APPLICATION_KEY
        sync: false
      - key: ATTEST_SIGNING_KEY_HEX
        sync: false
      - key: ATTEST_VERIFY_KEY_HEX
        sync: false
      - key: GMI_API_KEY
        sync: false
