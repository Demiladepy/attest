# genblaze 0.4.5 line (core 0.3.8) — BaseProvider validation, GMICloud preflight,
# clearer errors, verify --fetch hardening
genblaze-core>=0.3.8
genblaze-s3>=0.3.6
genblaze-gmicloud>=0.3.5
cryptography>=42.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
httpx>=0.28.0
python-multipart>=0.0.12
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
boto3>=1.35.0
b2sdk>=2.5.0
Pillow>=10.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Deploy tooling (local only — not needed at runtime)
# huggingface_hub  # for attest.scripts.deploy_hf_space (HF Space deploy)

# Optional provider packs
# genblaze-replicate  # when REPLICATE_API_TOKEN configured

# Compliance embeds (install when building full pipeline)
# c2pa-python
# trustmark
